name: Source publication checks
on: [push, pull_request]
permissions:
  contents: read
jobs:
  source-boundaries:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check source and bundled catalog boundaries
        run: python3 Scripts/validate_source_release.py
